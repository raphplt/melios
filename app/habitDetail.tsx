import { View, Text } from "react-native";

export default function HabitDetail({ route }: any) {
	// const { habit, habitInfos } = route.params || {};
	return (
		<View>
			{/* <Text>{habitInfos && habitInfos.name}</Text> */}
			<Text>Detail</Text>
		</View>
	);
}
